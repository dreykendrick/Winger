# Winger Backend V2 – Audit Center Specification

Immutable audit logging for all critical platform actions.

---

## 1. Audit Log Fields
`actor_profile_id`, `action`, `resource_type`, `resource_id`, `before_state`, `after_state`, `ip_hash`, `correlation_id`, `timestamp`.
