# Organization Context Specification

An **Organization** represents a legal business entity that owns one or more multi-tenant Workspaces.

## Features

- Loading user's active organization memberships
- Selecting active organization context
- Organization switching
- Persisting selected organization ID in `SecureStorageService`
