# Winger Backend V2 – Centralized Configuration Specification

Runtime settings and environment configuration management.

---

## 1. Storage
`ops.system_config` table storing key-value pairs with secret flags (`is_secret`).
